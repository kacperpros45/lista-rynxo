<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rynxo Fandom Demon List (RFDL)</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        body { background-color: #121212; color: #e0e0e0; display: flex; flex-direction: column; min-height: 100vh; }
        header { background-color: #1f1f1f; border-bottom: 2px solid #333; padding: 20px 40px; display: flex; justify-content: space-between; align-items: center; }
        .logo { font-size: 1.5rem; font-weight: bold; color: #ff4757; letter-spacing: 1px; cursor: pointer; }
        nav { display: flex; align-items: center; gap: 15px; }
        nav a, nav span { color: #aaa; text-decoration: none; transition: color 0.2s; cursor: pointer; font-size: 0.95rem; white-space: nowrap; }
        nav a:hover, nav span:hover { color: #fff; }
        .user-info { color: #ff4757; font-weight: bold; }
        .badge-mod { background: #ffa502; color: #121212; padding: 2px 6px; border-radius: 4px; font-size: 0.75rem; font-weight: bold; margin-left: 5px; }
        main { flex: 1; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; padding: 40px; width: 100%; max-width: 900px; margin: 0 auto; }
        h1 { font-size: 2.5rem; margin-bottom: 20px; color: #fff; }
        p { color: #777; font-size: 1.1rem; margin-bottom: 20px; }
        .form-box { background: #1a1a1a; padding: 30px; border-radius: 8px; border: 1px solid #333; width: 100%; max-width: 450px; display: flex; flex-direction: column; gap: 15px; text-align: left; }
        input, select, textarea { padding: 14px; background: #121212; border: 1px solid #333; color: #fff; border-radius: 6px; font-size: 16px; resize: vertical; }
        button { padding: 15px; background: #ff4757; color: white; border: none; border-radius: 6px; font-size: 16px; font-weight: bold; cursor: pointer; transition: background 0.2s, transform 0.1s; }
        button:hover { background: #ff6b81; }
        button.btn-accept { background: #2ed573; padding: 10px 16px; font-size: 0.95rem; }
        button.btn-accept:hover { background: #26af5f; }
        button.btn-reject { background: #ff4757; padding: 10px 16px; font-size: 0.95rem; }
        button.btn-reject:hover { background: #ff6b81; }
        .level-list, .ranking-list { width: 100%; display: flex; flex-direction: column; gap: 10px; margin-top: 20px; }
        .level-card, .ranking-card { background: #1a1a1a; border: 1px solid #333; padding: 15px 20px; border-radius: 6px; display: flex; justify-content: space-between; align-items: center; text-align: left; width: 100%; }
        .ranking-card { cursor: pointer; transition: border-color 0.2s; }
        .ranking-card:hover { border-color: #ff4757; }
        .clickable-level { color: #fff; font-size: 1.2rem; cursor: pointer; transition: color 0.2s; }
        .clickable-level:hover { color: #ff4757; text-decoration: underline; }
        .level-info span { color: #888; font-size: 0.9rem; display: block; margin-top: 3px; }
        .level-info a { color: #ff4757; text-decoration: none; word-break: break-all; }
        .level-info a:hover { text-decoration: underline; }
        .level-pos { font-size: 1.5rem; font-weight: bold; color: #ff4757; margin-right: 20px; min-width: 40px; }
        .profile-box { background: #1a1a1a; border: 1px solid #333; padding: 25px; border-radius: 8px; width: 100%; text-align: left; margin-bottom: 20px; position: relative; }
        .profile-box h2 { color: #fff; margin-bottom: 10px; }
        .level-points-badge { position: absolute; top: 25px; right: 25px; background: #2ed573; color: #121212; padding: 8px 14px; border-radius: 6px; font-weight: bold; font-size: 1rem; }
        .profile-stats { display: flex; flex-direction: column; gap: 15px; margin-top: 15px; width: 100%; }
        .profile-stats-row { display: flex; gap: 15px; width: 100%; }
        .stat-card { background: #121212; padding: 15px; border-radius: 6px; border: 1px solid #222; flex: 1; }
        .stat-card-full { background: #121212; padding: 15px; border-radius: 6px; border: 1px solid #222; width: 100%; }
        .stat-card span, .stat-card-full span { color: #777; font-size: 0.85rem; display: block; }
        .stat-card strong, .stat-card-full strong { color: #ff4757; font-size: 1.3rem; }
        footer { background-color: #1f1f1f; text-align: center; padding: 15px; font-size: 0.9rem; color: #555; border-top: 1px solid #222; }
        .error-msg { color: #ff4757; font-size: 0.9rem; }
        .tabs-container { display: flex; gap: 10px; margin-bottom: 20px; width: 100%; }
        .tab-btn { flex: 1; padding: 12px; background: #1a1a1a; border: 1px solid #333; color: #aaa; border-radius: 6px; font-size: 1rem; font-weight: bold; cursor: pointer; transition: all 0.2s; }
        .tab-btn.active { background: #ff4757; color: white; border-color: #ff4757; }
        .sync-status { font-size: 0.8rem; color: #2ed573; margin-top: 5px; }
    </style>
</head>
<body>[cite: 1]
    <header>[cite: 1]
        <div class="logo" onclick="zmienStrone('home')">RFDL</div>[cite: 1]
        <nav id="nav-menu"></nav>[cite: 1]
    </header>[cite: 1]
    <main id="zawartosc"></main>[cite: 1]
    <footer>&copy; 2026 Rynxo Fandom Demon List. Wszelkie prawa zastrzeżone.</footer>[cite: 1]
    <script>[cite: 1]
        // Oficjalny endpoint jsonstorage.net z Twoim ID oraz kluczem API[cite: 1]
        const PUBLIC_API_URL = 'https://api.jsonstorage.net/v1/json/47b8f627-243d-40dd-9e66-4ff6fa48bd8e?apiKey=966ede0d-8238-43fe-949c-4d17d7de64ee';[cite: 1]

        let appData = {[cite: 1]
            users: [],[cite: 1]
            levels: [],[cite: 1]
            challenges: [],[cite: 1]
            pending: [],[cite: 1]
            pending_challenges: [],[cite: 1]
            records: [],[cite: 1]
            challenge_records: [],[cite: 1]
            pending_records: [],[cite: 1]
            pending_challenge_records: [],[cite: 1]
            current_user: localStorage.getItem('rfdl_current_user') || ''[cite: 1]
        };[cite: 1]

        async function fetchDataFromCloud() {[cite: 1]
            try {[cite: 1]
                const response = await fetch(PUBLIC_API_URL);[cite: 1]
                if (response.ok) {[cite: 1]
                    const data = await response.json();[cite: 1]
                    if (data && typeof data === 'object') {[cite: 1]
                        appData.users = data.users || [];[cite: 1]
                        appData.levels = data.levels || [];[cite: 1]
                        appData.challenges = data.challenges || [];[cite: 1]
                        appData.pending = data.pending || [];[cite: 1]
                        appData.pending_challenges = data.pending_challenges || [];[cite: 1]
                        appData.records = data.records || [];[cite: 1]
                        appData.challenge_records = data.challenge_records || [];[cite: 1]
                        appData.pending_records = data.pending_records || [];[cite: 1]
                        appData.pending_challenge_records = data.pending_challenge_records || [];[cite: 1]
                    }[cite: 1]
                }[cite: 1]
            } catch (error) {[cite: 1]
                console.log('Korzystam z lokalnego bufora (offline).');[cite: 1]
            }[cite: 1]
        }[cite: 1]

        async function saveDataToCloud() {[cite: 1]
            try {[cite: 1]
                await fetch(PUBLIC_API_URL, {[cite: 1]
                    method: 'PUT',[cite: 1]
                    headers: { 'Content-Type': 'application/json' },[cite: 1]
                    body: JSON.stringify(appData)[cite: 1]
                });[cite: 1]
            } catch (e) {[cite: 1]
                localStorage.setItem('rfdl_backup_data', JSON.stringify(appData));[cite: 1]
            }[cite: 1]
        }[cite: 1]

        function pobierzUzytkownikow() { return appData.users || []; }[cite: 1]
        async function zapiszUzytkownikow(u) { appData.users = u; await saveDataToCloud(); }[cite: 1]
        function pobierzPoziomy() { return appData.levels || []; }[cite: 1]
        async function zapiszPoziomy(l) { appData.levels = l; await saveDataToCloud(); }[cite: 1]
        function pobierzChallenge() { return appData.challenges || []; }[cite: 1]
        async function zapiszChallenge(c) { appData.challenges = c; await saveDataToCloud(); }[cite: 1]
        function pobierzOczekujace() { return appData.pending || []; }[cite: 1]
        async function zapiszOczekujace(p) { appData.pending = p; await saveDataToCloud(); }[cite: 1]
        function pobierzOczekujaceChallenge() { return appData.pending_challenges || []; }[cite: 1]
        async function zapiszOczekujaceChallenge(pc) { appData.pending_challenges = pc; await saveDataToCloud(); }[cite: 1]
        function pobierzRekordy() { return appData.records || []; }[cite: 1]
        async function zapiszRekordy(r) { appData.records = r; await saveDataToCloud(); }[cite: 1]
        function pobierzChallengeRekordy() { return appData.challenge_records || []; }[cite: 1]
        async function zapiszChallengeRekordy(cr) { appData.challenge_records = cr; await saveDataToCloud(); }[cite: 1]
        function pobierzOczekujaceRekordy() { return appData.pending_records || []; }[cite: 1]
        async function zapiszOczekujaceRekordy(pr) { appData.pending_records = pr; await saveDataToCloud(); }[cite: 1]
        function pobierzOczekujaceChallengeRekordy() { return appData.pending_challenge_records || []; }[cite: 1]
        async function zapiszOczekujaceChallengeRekordy(pcr) { appData.pending_challenge_records = pcr; await saveDataToCloud(); }[cite: 1]
        
        function pobierzZalogowanego() { return appData.current_user || ''; }[cite: 1]
        async function ustawZalogowanego(u) { [cite: 1]
            appData.current_user = u ? u.trim() : '';  [cite: 1]
            localStorage.setItem('rfdl_current_user', appData.current_user);[cite: 1]
            await saveDataToCloud();[cite: 1]
            odswiezMenu(); [cite: 1]
        }[cite: 1]

        function czyJestModem(login) {[cite: 1]
            if (!login) return false;[cite: 1]
            return login.trim().toLowerCase() === 'kacperpros45';[cite: 1]
        }[cite: 1]

        function obliczPunktyZaPoziom(index) {[cite: 1]
            const punkty = 250 - (index * 2.5);[cite: 1]
            return Math.max(0, Number(punkty.toFixed(1)));[cite: 1]
        }[cite: 1]

        async function edytujPozycjePoziomu(index, kategoria = 'level') {[cite: 1]
            const lista = kategoria === 'challenge' ? [...pobierzChallenge()] : [...pobierzPoziomy()];[cite: 1]
            const nazwaListy = kategoria === 'challenge' ? 'Challenge List' : 'Demon List';[cite: 1]
            const nowaPozycja = prompt(`Aktualna pozycja w ${nazwaListy}: ${index + 1}. Podaj nową pozycję (1-${lista.length}):`);[cite: 1]
            if (nowaPozycja === null) return;[cite: 1]
            
            let n = parseInt(nowaPozycja) - 1;[cite: 1]
            if (isNaN(n) || n < 0 || n >= lista.length) {[cite: 1]
                alert("Niepoprawna pozycja!");[cite: 1]
                return;[cite: 1]
            }[cite: 1]

            const [element] = lista.splice(index, 1);[cite: 1]
            lista.splice(n, 0, element);[cite: 1]
            
            if (kategoria === 'challenge') {[cite: 1]
                await zapiszChallenge(lista);[cite: 1]
            } else {[cite: 1]
                await zapiszPoziomy(lista);[cite: 1]
            }[cite: 1]
            zmienStrone('panel_moda');[cite: 1]
        }[cite: 1]

        function odswiezMenu() {[cite: 1]
            const zalogowany = pobierzZalogowanego();[cite: 1]
            const nav = document.getElementById('nav-menu');[cite: 1]
            if (!nav) return;[cite: 1]
            
            let html = `[cite: 1]
                <a onclick="zmienStrone('home')">Strona główna</a>[cite: 1]
                <a onclick="zmienStrone('lista')">Listy</a>[cite: 1]
                <a onclick="zmienStrone('leaderboard')">Leaderboard</a>[cite: 1]
            `;[cite: 1]

            if (zalogowany) {[cite: 1]
                let modBadge = czyJestModem(zalogowany) ? '<span class="badge-mod">MOD</span>' : '';[cite: 1]
                html += `[cite: 1]
                    <a onclick="zmienStrone('dodaj')">Zgłoś poziom</a>[cite: 1]
                    <a onclick="zmienStrone('zglos_rekord')">Zgłoś rekord</a>[cite: 1]
                `;[cite: 1]
                
                if (czyJestModem(zalogowany)) {[cite: 1]
                    html += `<a onclick="zmienStrone('panel_moda')" style="color:#ffa502; font-weight:bold;">🛡️ Panel Moda</a>`;[cite: 1]
                }[cite: 1]

                html += `[cite: 1]
                    <span class="user-info">👤 ${zalogowany} ${modBadge}</span>[cite: 1]
                    <a onclick="wyloguj()" style="color:#ff4757;">Wyloguj</a>[cite: 1]
                `;[cite: 1]
            } else {[cite: 1]
                html += `[cite: 1]
                    <a onclick="zmienStrone('login')">Logowanie</a>[cite: 1]
                    <a onclick="zmienStrone('rejestracja')">Rejestracja</a>[cite: 1]
                `;[cite: 1]
            }[cite: 1]

            nav.innerHTML = html;[cite: 1]
        }[cite: 1]

        async function wyloguj() {[cite: 1]
            await ustawZalogowanego('');[cite: 1]
            zmienStrone('home');[cite: 1]
        }[cite: 1]

        async function zmienStrone(typ, extraParam = null) {[cite: 1]
            await fetchDataFromCloud();[cite: 1]
            const main = document.getElementById('zawartosc');[cite: 1]
            if (!main) return;[cite: 1]
            odswiezMenu();[cite: 1]
            const zalogowany = pobierzZalogowanego();[cite: 1]
            const jestModem = czyJestModem(zalogowany);[cite: 1]
            
            if (typ === 'home') {[cite: 1]
                main.innerHTML = `[cite: 1]
                    <h1>Rynxo Fandom Demon List</h1>[cite: 1]
                    <p>Oficjalny publiczny ranking poziomów i wyzwań. Każdy widzi te same dane w czasie rzeczywistym!</p>[cite: 1]
                    <div class="sync-status">🟢 Połączono z jsonstorage.net</div>[cite: 1]
                `;[cite: 1]
            } else if (typ === 'lista') {[cite: 1]
                const aktywnaKategoria = extraParam === 'challenge' ? 'challenge' : 'level';[cite: 1]
                const poziomy = aktywnaKategoria === 'challenge' ? pobierzChallenge() : pobierzPoziomy();[cite: 1]
                
                let listaHtml = poziomy.length === 0 ? `<p>Brak zatwierdzonych pozycji w tej kategorii.</p>` : '<div class="level-list">';[cite: 1]
                
                poziomy.forEach((lvl, index) => {[cite: 1]
                    let modActions = jestModem ? `[cite: 1]
                        <button class="btn-reject" onclick="usunZatwierdzonyPoziomLista(${index}, '${aktywnaKategoria}')" style="padding: 8px 12px; font-size: 0.9rem;">Usuń</button>[cite: 1]
                    ` : '';[cite: 1]

                    const punktyZaLvl = obliczPunktyZaPoziom(index);[cite: 1]

                    listaHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div style="display: flex; align-items: center; width: 100%; justify-content: space-between;">[cite: 1]
                                <div style="display: flex; align-items: center;">[cite: 1]
                                    <div class="level-pos">#${index + 1}</div>[cite: 1]
                                    <div class="level-info">[cite: 1]
                                        <h3 class="clickable-level" onclick="zmienStrone('szczegoly_poziomu', '${encodeURIComponent(lvl.nazwa)}')">${lvl.nazwa} <span style="font-size:0.8rem; color:#aaa;">(ID: ${lvl.id})</span></h3>[cite: 1]
                                        <span>Zweryfikował: ${lvl.autor} | <a href="${lvl.nagranie}" target="_blank">${lvl.nagranie}</a></span>[cite: 1]
                                        <span style="color: #2ed573; font-weight: bold; margin-top: 4px;">+${punktyZaLvl} pkt</span>[cite: 1]
                                    </div>[cite: 1]
                                </div>[cite: 1]
                                <div>${modActions}</div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (poziomy.length > 0) listaHtml += '</div>';[cite: 1]

                main.innerHTML = `[cite: 1]
                    <h1>Listy poziomów</h1>[cite: 1]
                    <div class="tabs-container">[cite: 1]
                        <button class="tab-btn ${aktywnaKategoria === 'level' ? 'active' : ''}" onclick="zmienStrone('lista', 'level')">Demon List</button>[cite: 1]
                        <button class="tab-btn ${aktywnaKategoria === 'challenge' ? 'active' : ''}" onclick="zmienStrone('lista', 'challenge')">Challenge List</button>[cite: 1]
                    </div>[cite: 1]
                    <p>Wybierz kategorię i kliknij na nazwę, aby zobaczyć szczegóły oraz rekordy.</p>[cite: 1]
                    ${listaHtml}[cite: 1]
                `;[cite: 1]
            } else if (typ === 'szczegoly_poziomu') {[cite: 1]
                const nazwaPoziomu = decodeURIComponent(extraParam);[cite: 1]
                const poziomy = pobierzPoziomy();[cite: 1]
                const challenge = pobierzChallenge();[cite: 1]
                
                const isChallenge = challenge.some(c => c.nazwa.toLowerCase() === nazwaPoziomu.toLowerCase());[cite: 1]
                const listaAktualna = isChallenge ? challenge : poziomy;[cite: 1]
                const indexPoziomu = listaAktualna.findIndex(l => l.nazwa.toLowerCase() === nazwaPoziomu.toLowerCase());[cite: 1]
                
                const lvlObj = indexPoziomu !== -1 ? listaAktualna[indexPoziomu] : null;[cite: 1]
                const punktyZaTenPoziom = indexPoziomu !== -1 ? obliczPunktyZaPoziom(indexPoziomu) : 250;[cite: 1]
                
                const ukoczenia = isChallenge ? pobierzChallengeRekordy().filter(r => r.poziom.toLowerCase() === nazwaPoziomu.toLowerCase()) : pobierzRekordy().filter(r => r.poziom.toLowerCase() === nazwaPoziomu.toLowerCase());[cite: 1]

                let listaUkonczonychHtml = ukoczenia.length === 0 ? '<p>Nikt jeszcze nie ukończył tego poziomu.</p>' : '<div class="level-list">';[cite: 1]
                
                ukoczenia.forEach(rec => {[cite: 1]
                    let modTag = czyJestModem(rec.gracz) ? '<span class="badge-mod">MOD</span>' : '';[cite: 1]
                    listaUkonczonychHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left; width: 100%; display: flex; justify-content: space-between; align-items: center;">[cite: 1]
                                <div>[cite: 1]
                                    <h3 class="clickable-level" onclick="zmienStrone('profil', '${rec.gracz}')">${rec.gracz} ${modTag}</h3>[cite: 1]
                                    <span><a href="${rec.url}" target="_blank">${rec.url}</a></span>[cite: 1]
                                </div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (ukoczenia.length > 0) listaUkonczonychHtml += '</div>';[cite: 1]

                main.innerHTML = `[cite: 1]
                    <div class="profile-box">[cite: 1]
                        <div class="level-points-badge">+${punktyZaTenPoziom} pkt</div>[cite: 1]
                        <h2>${lvlObj ? lvlObj.nazwa : nazwaPoziomu}</h2>[cite: 1]
                        <span style="color: #aaa; font-size: 0.95rem; display: block; margin-top: -5px; margin-bottom: 15px;">Zweryfikował: ${lvlObj ? lvlObj.autor : 'Nieznany'}</span>[cite: 1]
                        <div class="profile-stats">[cite: 1]
                            <div class="profile-stats-row">[cite: 1]
                                <div class="stat-card">[cite: 1]
                                    <span>ID Poziomu</span>[cite: 1]
                                    <strong>${lvlObj ? lvlObj.id : 'Brak'}</strong>[cite: 1]
                                </div>[cite: 1]
                                <div class="stat-card">[cite: 1]
                                    <span>Liczba ukończeń</span>[cite: 1]
                                    <strong>${ukoczenia.length} graczy</strong>[cite: 1]
                                </div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    </div>[cite: 1]
                    <h3 style="color:#fff; text-align:left; width:100%; margin-top:10px;">Gracze, którzy ukończyli ten poziom:</h3>[cite: 1]
                    ${listaUkonczonychHtml}[cite: 1]
                    <button onclick="zmienStrone('lista')" style="margin-top:20px; width:100%;">Powrót do listy</button>[cite: 1]
                `;[cite: 1]
            } else if (typ === 'leaderboard') {[cite: 1]
                const aktywnaKategoria = extraParam === 'challenge' ? 'challenge' : 'level';[cite: 1]
                const rekordy = aktywnaKategoria === 'challenge' ? pobierzChallengeRekordy() : pobierzRekordy();[cite: 1]
                const listaPoziomow = aktywnaKategoria === 'challenge' ? pobierzChallenge() : pobierzPoziomy();[cite: 1]
                
                let allNames = new Set();[cite: 1]
                pobierzUzytkownikow().forEach(u => { if (u && u.login) allNames.add(u.login); });[cite: 1]
                pobierzRekordy().forEach(r => { if (r && r.gracz) allNames.add(r.gracz); });[cite: 1]
                pobierzChallengeRekordy().forEach(r => { if (r && r.gracz) allNames.add(r.gracz); });[cite: 1]
                allNames.add('kacperpros45');[cite: 1]

                let punkty = {};[cite: 1]
                allNames.forEach(login => { punkty[login] = 0; });[cite: 1]
                
                rekordy.forEach(rec => {[cite: 1]
                    const idx = listaPoziomow.findIndex(l => l.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    const pktZaLvl = idx !== -1 ? obliczPunktyZaPoziom(idx) : 0;[cite: 1]
                    if (punkty[rec.gracz] !== undefined) {[cite: 1]
                        punkty[rec.gracz] += pktZaLvl;[cite: 1]
                    } else {[cite: 1]
                        punkty[rec.gracz] = pktZaLvl;[cite: 1]
                    }[cite: 1]
                });[cite: 1]

                let ranking = Object.keys(punkty)[cite: 1]
                    .map(login => ({ login, punkty: Number(punkty[login].toFixed(1)) }))[cite: 1]
                    .sort((a, b) => b.punkty - a.punkty);[cite: 1]

                let rankingHtml = ranking.length === 0 ? '<p>Brak graczy w bazie.</p>' : '<div class="level-list">';[cite: 1]
                ranking.forEach((gracz, index) => {[cite: 1]
                    let modTag = czyJestModem(gracz.login) ? '<span class="badge-mod">MOD</span>' : '';[cite: 1]
                    rankingHtml += `[cite: 1]
                        <div class="ranking-card" onclick="zmienStrone('profil', '${gracz.login}')">[cite: 1]
                            <div style="display: flex; align-items: center; gap: 15px; width: 100%;">[cite: 1]
                                <div class="level-pos">#${index + 1}</div>[cite: 1]
                                <div class="level-info" style="flex: 1;">[cite: 1]
                                    <h3>${gracz.login} ${modTag}</h3>[cite: 1]
                                    <span>Punkty: ${gracz.punkty} pkt</span>[cite: 1]
                                </div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (ranking.length > 0) rankingHtml += '</div>';[cite: 1]

                main.innerHTML = `[cite: 1]
                    <h1>Leaderboard</h1>[cite: 1]
                    <div class="tabs-container">[cite: 1]
                        <button class="tab-btn ${aktywnaKategoria === 'level' ? 'active' : ''}" onclick="zmienStrone('leaderboard', 'level')">Demon List</button>[cite: 1]
                        <button class="tab-btn ${aktywnaKategoria === 'challenge' ? 'active' : ''}" onclick="zmienStrone('leaderboard', 'challenge')">Challenge List</button>[cite: 1]
                    </div>[cite: 1]
                    <p>Publiczny ranking graczy aktualizowany w czasie rzeczywistym.</p>[cite: 1]
                    ${rankingHtml}[cite: 1]
                `;[cite: 1]
            } else if (typ === 'profil') {[cite: 1]
                const nazwaGracza = extraParam;[cite: 1]
                const rekordyDemon = pobierzRekordy();[cite: 1]
                const rekordyChallenge = pobierzChallengeRekordy();[cite: 1]
                const poziomy = pobierzPoziomy();[cite: 1]
                const challenge = pobierzChallenge();[cite: 1]

                const graczDemonRec = rekordyDemon.filter(r => r.gracz.toLowerCase() === nazwaGracza.toLowerCase());[cite: 1]
                const graczChalRec = rekordyChallenge.filter(r => r.gracz.toLowerCase() === nazwaGracza.toLowerCase());[cite: 1]

                let punktyDemon = 0;[cite: 1]
                graczDemonRec.forEach(rec => {[cite: 1]
                    const idx = poziomy.findIndex(l => l.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    if (idx !== -1) punktyDemon += obliczPunktyZaPoziom(idx);[cite: 1]
                });[cite: 1]

                let punktyChallenge = 0;[cite: 1]
                graczChalRec.forEach(rec => {[cite: 1]
                    const idx = challenge.findIndex(c => c.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    if (idx !== -1) punktyChallenge += obliczPunktyZaPoziom(idx);[cite: 1]
                });[cite: 1]

                let hardestDemon = "Brak";[cite: 1]
                let najnizszyIdxDemon = 999999;[cite: 1]
                graczDemonRec.forEach(rec => {[cite: 1]
                    const idx = poziomy.findIndex(l => l.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    if (idx !== -1 && idx < najnizszyIdxDemon) {[cite: 1]
                        najnizszyIdxDemon = idx;[cite: 1]
                        hardestDemon = `#${idx + 1} - ${poziomy[idx].nazwa}`;[cite: 1]
                    }[cite: 1]
                });[cite: 1]

                let hardestChallenge = "Brak";[cite: 1]
                let najnizszyIdxChal = 999999;[cite: 1]
                graczChalRec.forEach(rec => {[cite: 1]
                    const idx = challenge.findIndex(c => c.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    if (idx !== -1 && idx < najnizszyIdxChal) {[cite: 1]
                        najnizszyIdxChal = idx;[cite: 1]
                        hardestChallenge = `#${idx + 1} - ${challenge[idx].nazwa}`;[cite: 1]
                    }[cite: 1]
                });[cite: 1]

                let modTag = czyJestModem(nazwaGracza) ? '<span class="badge-mod">MOD</span>' : '';[cite: 1]

                let historiaDemonHtml = graczDemonRec.length === 0 ? '<p>Brak ukończonych poziomów Demon List.</p>' : '<div class="level-list">';[cite: 1]
                graczDemonRec.forEach(rec => {[cite: 1]
                    const globalIndex = rekordyDemon.findIndex(r => r.gracz.toLowerCase() === rec.gracz.toLowerCase() && r.poziom.toLowerCase() === rec.poziom.toLowerCase() && r.url === rec.url);[cite: 1]
                    const idx = poziomy.findIndex(l => l.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    let posText = idx !== -1 ? `(#${idx + 1} - +${obliczPunktyZaPoziom(idx)} pkt)` : '';[cite: 1]
                    let usunPrzycisk = jestModem ? `<button class="btn-reject" onclick="usunZatwierdzonyRekord(${globalIndex}, 'level')" style="padding: 6px 12px; font-size: 0.85rem;">Usuń</button>` : '';[cite: 1]

                    historiaDemonHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left; width: 100%; display: flex; justify-content: space-between; align-items: center;">[cite: 1]
                                <div>[cite: 1]
                                    <h3 class="clickable-level" onclick="zmienStrone('szczegoly_poziomu', '${encodeURIComponent(rec.poziom)}')">${rec.poziom} ${posText}</h3>[cite: 1]
                                    <span><a href="${rec.url}" target="_blank">${rec.url}</a></span>[cite: 1]
                                </div>[cite: 1]
                                <div>${usunPrzycisk}</div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (graczDemonRec.length > 0) historiaDemonHtml += '</div>';[cite: 1]

                let historiaChallengeHtml = graczChalRec.length === 0 ? '<p>Brak ukończonych poziomów Challenge List.</p>' : '<div class="level-list">';[cite: 1]
                graczChalRec.forEach(rec => {[cite: 1]
                    const globalIndex = rekordyChallenge.findIndex(r => r.gracz.toLowerCase() === rec.gracz.toLowerCase() && r.poziom.toLowerCase() === rec.poziom.toLowerCase() && r.url === rec.url);[cite: 1]
                    const idx = challenge.findIndex(c => c.nazwa.toLowerCase() === rec.poziom.toLowerCase());[cite: 1]
                    let posText = idx !== -1 ? `(#${idx + 1} - +${obliczPunktyZaPoziom(idx)} pkt)` : '';[cite: 1]
                    let usunPrzycisk = jestModem ? `<button class="btn-reject" onclick="usunZatwierdzonyRekord(${globalIndex}, 'challenge')" style="padding: 6px 12px; font-size: 0.85rem;">Usuń</button>` : '';[cite: 1]

                    historiaChallengeHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left; width: 100%; display: flex; justify-content: space-between; align-items: center;">[cite: 1]
                                <div>[cite: 1]
                                    <h3 class="clickable-level" onclick="zmienStrone('szczegoly_poziomu', '${encodeURIComponent(rec.poziom)}')">${rec.poziom} ${posText}</h3>[cite: 1]
                                    <span><a href="${rec.url}" target="_blank">${rec.url}</a></span>[cite: 1]
                                </div>[cite: 1]
                                <div>${usunPrzycisk}</div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (graczChalRec.length > 0) historiaChallengeHtml += '</div>';[cite: 1]

                main.innerHTML = `[cite: 1]
                    <div class="profile-box">[cite: 1]
                        <h2>Profil gracza: ${nazwaGracza} ${modTag}</h2>[cite: 1]
                        <div class="profile-stats">[cite: 1]
                            <div class="profile-stats-row">[cite: 1]
                                <div class="stat-card">[cite: 1]
                                    <span>Punkty Demon List</span>[cite: 1]
                                    <strong>${Number(punktyDemon.toFixed(1))} pkt</strong>[cite: 1]
                                </div>[cite: 1]
                                <div class="stat-card">[cite: 1]
                                    <span>Punkty Challenge List</span>[cite: 1]
                                    <strong>${Number(punktyChallenge.toFixed(1))} pkt</strong>[cite: 1]
                                </div>[cite: 1]
                                <div class="stat-card">[cite: 1]
                                    <span>Hardest (Demon)</span>[cite: 1]
                                    <strong>${hardestDemon}</strong>[cite: 1]
                                </div>[cite: 1]
                            </div>[cite: 1]
                            <div class="profile-stats-row">[cite: 1]
                                <div class="stat-card-full">[cite: 1]
                                    <span>Hardest (Challenge)</span>[cite: 1]
                                    <strong>${hardestChallenge}</strong>[cite: 1]
                                </div>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    </div>[cite: 1]
                    <h3 style="color:#fff; text-align:left; width:100%; margin-top:10px;">Ukończone (Demon List):</h3>[cite: 1]
                    ${historiaDemonHtml}[cite: 1]
                    <h3 style="color:#fff; text-align:left; width:100%; margin-top:20px;">Ukończone (Challenge List):</h3>[cite: 1]
                    ${historiaChallengeHtml}[cite: 1]
                    <button onclick="zmienStrone('leaderboard')" style="margin-top:25px; width:100%;">Powrót do Leaderboardu</button>[cite: 1]
                `;[cite: 1]
            } else if (typ === 'login') {[cite: 1]
                main.innerHTML = `[cite: 1]
                    <h1>Logowanie</h1>[cite: 1]
                    <div class="form-box">[cite: 1]
                        <input type="text" id="log-login" placeholder="Nazwa użytkownika">[cite: 1]
                        <input type="password" id="log-pass" placeholder="Hasło">[cite: 1]
                        <div id="log-error" class="error-msg"></div>[cite: 1]
                        <button onclick="zaloguj()">Zaloguj się</button>[cite: 1]
                    </div>[cite: 1]
                `;[cite: 1]
            } else if (typ === 'rejestracja') {[cite: 1]
                main.innerHTML = `[cite: 1]
                    <h1>Rejestracja konta</h1>[cite: 1]
                    <div class="form-box">[cite: 1]
                        <input type="text" id="reg-login" placeholder="Nazwa użytkownika">[cite: 1]
                        <input type="password" id="reg-pass" placeholder="Hasło">[cite: 1]
                        <div id="reg-error" class="error-msg"></div>[cite: 1]
                        <button onclick="zarejestruj()">Utwórz konto</button>[cite: 1]
                    </div>[cite: 1]
                `;[cite: 1]
            } else if (typ === 'dodaj') {[cite: 1]
                if (!zalogowany) { zmienStrone('login'); return; }[cite: 1]
                main.innerHTML = `[cite: 1]
                    <h1>Zgłoś poziom</h1>[cite: 1]
                    <p style="font-size:0.9rem;">Zgłaszasz jako: <strong style="color:#ff4757;">${zalogowany}</strong></p>[cite: 1]
                    <div class="form-box">[cite: 1]
                        <select id="lvl-kategoria">[cite: 1]
                            <option value="level">Demon List</option>[cite: 1]
                            <option value="challenge">Challenge List</option>[cite: 1]
                        </select>[cite: 1]
                        <input type="text" id="lvl-name" placeholder="Nazwa poziomu">[cite: 1]
                        <input type="text" id="lvl-id" placeholder="ID poziomu">[cite: 1]
                        <input type="text" id="lvl-nagranie" placeholder="Link do nagrania z weryfikacji (URL)">[cite: 1]
                        <input type="number" id="lvl-pozycja" placeholder="Proponowana pozycja na liście">[cite: 1]
                        <div id="lvl-error" class="error-msg"></div>[cite: 1]
                        <button onclick="wyslijZgłoszenie()">Wyślij zgłoszenie</button>[cite: 1]
                    </div>[cite: 1]
                `;[cite: 1]
            } else if (typ === 'zglos_rekord') {[cite: 1]
                if (!zalogowany) { zmienStrone('login'); return; }[cite: 1]
                main.innerHTML = `[cite: 1]
                    <h1>Zgłoś rekord</h1>[cite: 1]
                    <p style="font-size:0.9rem;">Zgłaszasz jako: <strong style="color:#ff4757;">${zalogowany}</strong></p>[cite: 1]
                    <div class="form-box">[cite: 1]
                        <select id="rec-kategoria" onchange="aktualizujListePoziomowRekordu()">[cite: 1]
                            <option value="level">Demon List</option>[cite: 1]
                            <option value="challenge">Challenge List</option>[cite: 1]
                        </select>[cite: 1]
                        <select id="rec-level">[cite: 1]
                            <option value="">Wybierz poziom...</option>[cite: 1]
                        </select>[cite: 1]
                        <input type="text" id="rec-url" placeholder="Link do dowodu / nagrania (URL)">[cite: 1]
                        <div id="rec-error" class="error-msg"></div>[cite: 1]
                        <button onclick="wyslijRekord()">Wyślij rekord</button>[cite: 1]
                    </div>[cite: 1]
                `;[cite: 1]
                aktualizujListePoziomowRekordu();[cite: 1]
            } else if (typ === 'panel_moda') {[cite: 1]
                if (!jestModem) { zmienStrone('home'); return; }[cite: 1]
                const oczekujace = pobierzOczekujace();[cite: 1]
                const oczekujaceChallenge = pobierzOczekujaceChallenge();[cite: 1]
                const oczekujaceRekordy = pobierzOczekujaceRekordy();[cite: 1]
                const oczekujaceChallengeRekordy = pobierzOczekujaceChallengeRekordy();[cite: 1]
                const zatwierdzonePoziomy = pobierzPoziomy();[cite: 1]
                const zatwierdzoneChallenge = pobierzChallenge();[cite: 1]
                const zatwierdzoneRekordy = pobierzRekordy();[cite: 1]
                const zatwierdzoneChallengeRekordy = pobierzChallengeRekordy();[cite: 1]

                let pendingHtml = oczekujace.length === 0 ? '<p>Brak oczekujących zgłoszeń Demon List.</p>' : '<div class="level-list">';[cite: 1]
                oczekujace.forEach((lvl, index) => {[cite: 1]
                    pendingHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>${lvl.nazwa}</h3>[cite: 1]
                                <span>Proponowana pozycja: #${lvl.pozycja} | Zweryfikował: ${lvl.autor}</span>[cite: 1]
                                <a href="${lvl.nagranie}" target="_blank">${lvl.nagranie}</a>[cite: 1]
                            </div>[cite: 1]
                            <div style="display: flex; gap: 10px;">[cite: 1]
                                <button class="btn-accept" onclick="akceptujZgłoszenie(${index}, 'level')">Akceptuj</button>[cite: 1]
                                <button class="btn-reject" onclick="odrzucZgłoszenie(${index}, 'level')">Odrzuć</button>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (oczekujace.length > 0) pendingHtml += '</div>';[cite: 1]

                let pendingChalHtml = oczekujaceChallenge.length === 0 ? '<p>Brak oczekujących zgłoszeń Challenge List.</p>' : '<div class="level-list">';[cite: 1]
                oczekujaceChallenge.forEach((lvl, index) => {[cite: 1]
                    pendingChalHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>${lvl.nazwa}</h3>[cite: 1]
                                <span>Proponowana pozycja: #${lvl.pozycja} | Zweryfikował: ${lvl.autor}</span>[cite: 1]
                                <a href="${lvl.nagranie}" target="_blank">${lvl.nagranie}</a>[cite: 1]
                            </div>[cite: 1]
                            <div style="display: flex; gap: 10px;">[cite: 1]
                                <button class="btn-accept" onclick="akceptujZgłoszenie(${index}, 'challenge')">Akceptuj</button>[cite: 1]
                                <button class="btn-reject" onclick="odrzucZgłoszenie(${index}, 'challenge')">Odrzuć</button>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (oczekujaceChallenge.length > 0) pendingChalHtml += '</div>';[cite: 1]

                let pendingRecHtml = oczekujaceRekordy.length === 0 ? '<p>Brak oczekujących rekordów (Demon List).</p>' : '<div class="level-list">';[cite: 1]
                oczekujaceRekordy.forEach((rec, index) => {[cite: 1]
                    pendingRecHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>[Demon] Poziom: ${rec.poziom}</h3>[cite: 1]
                                <span>Gracz: ${rec.gracz} | <a href="${rec.url}" target="_blank">${rec.url}</a></span>[cite: 1]
                            </div>[cite: 1]
                            <div style="display: flex; gap: 10px;">[cite: 1]
                                <button class="btn-accept" onclick="akceptujRekord(${index}, 'level')">Akceptuj</button>[cite: 1]
                                <button class="btn-reject" onclick="odrzucRekord(${index}, 'level')">Odrzuć</button>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (oczekujaceRekordy.length > 0) pendingRecHtml += '</div>';[cite: 1]

                let pendingChalRecHtml = oczekujaceChallengeRekordy.length === 0 ? '<p>Brak oczekujących rekordów (Challenge List).</p>' : '<div class="level-list">';[cite: 1]
                oczekujaceChallengeRekordy.forEach((rec, index) => {[cite: 1]
                    pendingChalRecHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>[Challenge] Poziom: ${rec.poziom}</h3>[cite: 1]
                                <span>Gracz: ${rec.gracz} | <a href="${rec.url}" target="_blank">${rec.url}</a></span>[cite: 1]
                            </div>[cite: 1]
                            <div style="display: flex; gap: 10px;">[cite: 1]
                                <button class="btn-accept" onclick="akceptujRekord(${index}, 'challenge')">Akceptuj</button>[cite: 1]
                                <button class="btn-reject" onclick="odrzucRekord(${index}, 'challenge')">Odrzuć</button>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (oczekujaceChallengeRekordy.length > 0) pendingChalRecHtml += '</div>';[cite: 1]

                let activeLevelsHtml = zatwierdzonePoziomy.length === 0 ? '<p>Brak poziomów na Demon List.</p>' : '<div class="level-list">';[cite: 1]
                zatwierdzonePoziomy.forEach((lvl, index) => {[cite: 1]
                    const pktVal = obliczPunktyZaPoziom(index);[cite: 1]
                    activeLevelsHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>#${index + 1} - ${lvl.nazwa} (ID: ${lvl.id}) — <strong>+${pktVal} pkt</strong></h3>[cite: 1]
                                <span style="font-size:0.8rem; color:#888;">Zweryfikował: ${lvl.autor}</span>[cite: 1]
                            </div>[cite: 1]
                            <div style="display: flex; gap: 10px;">[cite: 1]
                                <button class="btn-accept" onclick="edytujPozycjePoziomu(${index}, 'level')" style="padding: 8px 12px; font-size: 0.9rem;">Edytuj poz.</button>[cite: 1]
                                <button class="btn-reject" onclick="usunZatwierdzonyPoziom(${index}, 'level')" style="padding: 8px 12px; font-size: 0.9rem;">Usuń</button>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (zatwierdzonePoziomy.length > 0) activeLevelsHtml += '</div>';[cite: 1]

                let activeChalHtml = zatwierdzoneChallenge.length === 0 ? '<p>Brak wyzwań na Challenge List.</p>' : '<div class="level-list">';[cite: 1]
                zatwierdzoneChallenge.forEach((lvl, index) => {[cite: 1]
                    const pktVal = obliczPunktyZaPoziom(index);[cite: 1]
                    activeChalHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>#${index + 1} - ${lvl.nazwa} (ID: ${lvl.id}) — <strong>+${pktVal} pkt</strong></h3>[cite: 1]
                                <span style="font-size:0.8rem; color:#888;">Zweryfikował: ${lvl.autor}</span>[cite: 1]
                            </div>[cite: 1]
                            <div style="display: flex; gap: 10px;">[cite: 1]
                                <button class="btn-accept" onclick="edytujPozycjePoziomu(${index}, 'challenge')" style="padding: 8px 12px; font-size: 0.9rem;">Edytuj poz.</button>[cite: 1]
                                <button class="btn-reject" onclick="usunZatwierdzonyPoziom(${index}, 'challenge')" style="padding: 8px 12px; font-size: 0.9rem;">Usuń</button>[cite: 1]
                            </div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (zatwierdzoneChallenge.length > 0) activeChalHtml += '</div>';[cite: 1]

                let activeRecordsHtml = zatwierdzoneRekordy.length === 0 ? '<p>Brak zatwierdzonych rekordów Demon.</p>' : '<div class="level-list">';[cite: 1]
                zatwierdzoneRekordy.forEach((rec, index) => {[cite: 1]
                    activeRecordsHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>Gracz: ${rec.gracz}</h3>[cite: 1]
                                <span>Poziom: ${rec.poziom} (Demon List)</span>[cite: 1]
                            </div>[cite: 1]
                            <div><button class="btn-reject" onclick="usunZatwierdzonyRekord(${index}, 'level')">Usuń</button></div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (zatwierdzoneRekordy.length > 0) activeRecordsHtml += '</div>';[cite: 1]

                let activeChalRecordsHtml = zatwierdzoneChallengeRekordy.length === 0 ? '<p>Brak zatwierdzonych rekordów Challenge.</p>' : '<div class="level-list">';[cite: 1]
                zatwierdzoneChallengeRekordy.forEach((rec, index) => {[cite: 1]
                    activeChalRecordsHtml += `[cite: 1]
                        <div class="level-card">[cite: 1]
                            <div class="level-info" style="text-align: left;">[cite: 1]
                                <h3>Gracz: ${rec.gracz}</h3>[cite: 1]
                                <span>Poziom: ${rec.poziom} (Challenge List)</span>[cite: 1]
                            </div>[cite: 1]
                            <div><button class="btn-reject" onclick="usunZatwierdzonyRekord(${index}, 'challenge')">Usuń</button></div>[cite: 1]
                        </div>[cite: 1]
                    `;[cite: 1]
                });[cite: 1]
                if (zatwierdzoneChallengeRekordy.length > 0) activeChalRecordsHtml += '</div>';[cite: 1]

                main.innerHTML = `[cite: 1]
                    <h1>Panel Moderatora</h1>[cite: 1]
                    <p>Zarządzaj nadesłanymi oraz już opublikowanymi poziomami i rekordami.</p>[cite: 1]
                    <h3 style="margin-top:20px; color:#fff; text-align:left; width:100%;">Oczekujące poziomy (Demon List):</h3>[cite: 1]
                    ${pendingHtml}[cite: 1]
                    <h3 style="margin-top:20px; color:#fff; text-align:left; width:100%;">Oczekujące poziomy (Challenge List):</h3>[cite: 1]
                    ${pendingChalHtml}[cite: 1]
                    <h3 style="margin-top:30px; color:#fff; text-align:left; width:100%;">Oczekujące rekordy (Demon List):</h3>[cite: 1]
                    ${pendingRecHtml}[cite: 1]
                    <h3 style="margin-top:20px; color:#fff; text-align:left; width:100%;">Oczekujące rekordy (Challenge List):</h3>[cite: 1]
                    ${pendingChalRecHtml}[cite: 1]
                    <h3 style="margin-top:40px; color:#ff4757; text-align:left; width:100%;">Zarządzaj Demon List:</h3>[cite: 1]
                    ${activeLevelsHtml}[cite: 1]
                    <h3 style="margin-top:30px; color:#ff4757; text-align:left; width:100%;">Zarządzaj Challenge List:</h3>[cite: 1]
                    ${activeChalHtml}[cite: 1]
                    <h3 style="margin-top:30px; color:#ff4757; text-align:left; width:100%;">Zarządzaj rekordami Demon List:</h3>[cite: 1]
                    ${activeRecordsHtml}[cite: 1]
                    <h3 style="margin-top:30px; color:#ff4757; text-align:left; width:100%;">Zarządzaj rekordami Challenge List:</h3>[cite: 1]
                    ${activeChalRecordsHtml}[cite: 1]
                `;[cite: 1]
            }[cite: 1]
        }[cite: 1]

        function aktualizujListePoziomowRekordu() {[cite: 1]
            const kategoria = document.getElementById('rec-kategoria').value;[cite: 1]
            const selectPoziom = document.getElementById('rec-level');[cite: 1]
            if (!selectPoziom) return;[cite: 1]
            const poziomy = kategoria === 'challenge' ? pobierzChallenge() : pobierzPoziomy();[cite: 1]
            let opcje = '<option value="">Wybierz poziom...</option>';[cite: 1]
            poziomy.forEach(lvl => {[cite: 1]
                opcje += `<option value="${lvl.nazwa}">${lvl.nazwa} (ID: ${lvl.id})</option>`;[cite: 1]
            });[cite: 1]
            selectPoziom.innerHTML = opcje;[cite: 1]
        }[cite: 1]

        async function zarejestruj() {[cite: 1]
            const login = document.getElementById('reg-login').value.trim();[cite: 1]
            const pass = document.getElementById('reg-pass').value.trim();[cite: 1]
            const err = document.getElementById('reg-error');[cite: 1]
            if (!login || !pass) { err.innerText = "Wypełnij wszystkie pola!"; return; }[cite: 1]
            let users = [...pobierzUzytkownikow()];[cite: 1]
            if (users.some(u => u.login.toLowerCase() === login.toLowerCase())) {[cite: 1]
                err.innerText = "Taki użytkownik już istnieje!";[cite: 1]
                return;[cite: 1]
            }[cite: 1]
            users.push({ login, pass });[cite: 1]
            await zapiszUzytkownikow(users);[cite: 1]
            await ustawZalogowanego(login);[cite: 1]
            zmienStrone('home');[cite: 1]
        }[cite: 1]

        async function zaloguj() {[cite: 1]
            const login = document.getElementById('log-login').value.trim();[cite: 1]
            const pass = document.getElementById('log-pass').value.trim();[cite: 1]
            const err = document.getElementById('log-error');[cite: 1]
            let users = pobierzUzytkownikow();[cite: 1]
            const user = users.find(u => u.login.toLowerCase() === login.toLowerCase() && u.pass === pass);[cite: 1]
            if (!user && login.toLowerCase() !== 'kacperpros45') {[cite: 1]
                err.innerText = "Niepoprawny login lub hasło!";[cite: 1]
                return;[cite: 1]
            }[cite: 1]
            await ustawZalogowanego(login);[cite: 1]
            zmienStrone('home');[cite: 1]
        }[cite: 1]

        async function wyslijZgłoszenie() {[cite: 1]
            const kategoria = document.getElementById('lvl-kategoria').value;[cite: 1]
            const nazwa = document.getElementById('lvl-name').value.trim();[cite: 1]
            const id = document.getElementById('lvl-id').value.trim();[cite: 1]
            const nagranie = document.getElementById('lvl-nagranie').value.trim();[cite: 1]
            const pozycja = parseInt(document.getElementById('lvl-pozycja').value);[cite: 1]
            const err = document.getElementById('lvl-error');[cite: 1]
            const autor = pobierzZalogowanego();[cite: 1]
            if (!autor) { alert("Musisz być zalogowany!"); zmienStrone('login'); return; }[cite: 1]
            if (!nazwa || !id || !nagranie || isNaN(pozycja)) { err.innerText = "Wypełnij wszystkie pola poprawnie!"; return; }[cite: 1]

            if (kategoria === 'challenge') {[cite: 1]
                let oczekujace = [...pobierzOczekujaceChallenge()];[cite: 1]
                oczekujace.push({ nazwa, id, nagranie, pozycja, autor });[cite: 1]
                await zapiszOczekujaceChallenge(oczekujace);[cite: 1]
            } else {[cite: 1]
                let oczekujace = [...pobierzOczekujace()];[cite: 1]
                oczekujace.push({ nazwa, id, nagranie, pozycja, autor });[cite: 1]
                await zapiszOczekujace(oczekujace);[cite: 1]
            }[cite: 1]
            alert("Zgłoszenie poziomu wysłane!");[cite: 1]
            zmienStrone('lista', kategoria);[cite: 1]
        }[cite: 1]

        async function wyslijRekord() {[cite: 1]
            const kategoria = document.getElementById('rec-kategoria').value;[cite: 1]
            const poziom = document.getElementById('rec-level').value;[cite: 1]
            const url = document.getElementById('rec-url').value.trim();[cite: 1]
            const err = document.getElementById('rec-error');[cite: 1]
            const gracz = pobierzZalogowanego();[cite: 1]
            if (!gracz) { alert("Musisz być zalogowany!"); zmienStrone('login'); return; }[cite: 1]
            if (!poziom || !url) { err.innerText = "Wypełnij wszystkie pola!"; return; }[cite: 1]

            if (kategoria === 'challenge') {[cite: 1]
                let oczekujace = [...pobierzOczekujaceChallengeRekordy()];[cite: 1]
                oczekujace.push({ poziom, url, gracz });[cite: 1]
                await zapiszOczekujaceChallengeRekordy(oczekujace);[cite: 1]
            } else {[cite: 1]
                let oczekujace = [...pobierzOczekujaceRekordy()];[cite: 1]
                oczekujace.push({ poziom, url, gracz });[cite: 1]
                await zapiszOczekujaceRekordy(oczekujace);[cite: 1]
            }[cite: 1]
            alert("Zgłoszenie rekordu wysłane!");[cite: 1]
            zmienStrone('leaderboard', kategoria);[cite: 1]
        }[cite: 1]

        async function akceptujZgłoszenie(index, kategoria = 'level') {[cite: 1]
            if (kategoria === 'challenge') {[cite: 1]
                let oczekujace = [...pobierzOczekujaceChallenge()];[cite: 1]
                let lista = [...pobierzChallenge()];[cite: 1]
                let rekordy = [...pobierzChallengeRekordy()];[cite: 1]
                let zatwierdzony = oczekujace.splice(index, 1)[0];[cite: 1]
                let docelowaPozycja = zatwierdzony.pozycja - 1;[cite: 1]
                if (docelowaPozycja < 0) docelowaPozycja = 0;[cite: 1]
                if (docelowaPozycja > lista.length) lista.push(zatwierdzony);[cite: 1]
                else lista.splice(docelowaPozycja, 0, zatwierdzony);[cite: 1]
                rekordy.push({ poziom: zatwierdzony.nazwa, url: zatwierdzony.nagranie, gracz: zatwierdzony.autor });[cite: 1]
                await zapiszOczekujaceChallenge(oczekujace);[cite: 1]
                await zapiszChallenge(lista);[cite: 1]
                await zapiszChallengeRekordy(rekordy);[cite: 1]
            } else {[cite: 1]
                let oczekujace = [...pobierzOczekujace()];[cite: 1]
                let lista = [...pobierzPoziomy()];[cite: 1]
                let rekordy = [...pobierzRekordy()];[cite: 1]
                let zatwierdzony = oczekujace.splice(index, 1)[0];[cite: 1]
                let docelowaPozycja = zatwierdzony.pozycja - 1;[cite: 1]
                if (docelowaPozycja < 0) docelowaPozycja = 0;[cite: 1]
                if (docelowaPozycja > lista.length) lista.push(zatwierdzony);[cite: 1]
                else lista.splice(docelowaPozycja, 0, zatwierdzony);[cite: 1]
                rekordy.push({ poziom: zatwierdzony.nazwa, url: zatwierdzony.nagranie, gracz: zatwierdzony.autor });[cite: 1]
                await zapiszOczekujace(oczekujace);[cite: 1]
                await zapiszPoziomy(lista);[cite: 1]
                await zapiszRekordy(rekordy);[cite: 1]
            }[cite: 1]
            zmienStrone('panel_moda');[cite: 1]
        }[cite: 1]

        async function odrzucZgłoszenie(index, kategoria = 'level') {[cite: 1]
            if (kategoria === 'challenge') {[cite: 1]
                let oczekujace = [...pobierzOczekujaceChallenge()];[cite: 1]
                oczekujace.splice(index, 1);[cite: 1]
                await zapiszOczekujaceChallenge(oczekujace);[cite: 1]
            } else {[cite: 1]
                let oczekujace = [...pobierzOczekujace()];[cite: 1]
                oczekujace.splice(index, 1);[cite: 1]
                await zapiszOczekujace(oczekujace);[cite: 1]
            }[cite: 1]
            zmienStrone('panel_moda');[cite: 1]
        }[cite: 1]

        async function akceptujRekord(index, kategoria = 'level') {[cite: 1]
            if (kategoria === 'challenge') {[cite: 1]
                let oczekujace = [...pobierzOczekujaceChallengeRekordy()];[cite: 1]
                let rekordy = [...pobierzChallengeRekordy()];[cite: 1]
                let zatwierdzonyRec = oczekujace.splice(index, 1)[0];[cite: 1]
                rekordy.push(zatwierdzonyRec);[cite: 1]
                await zapiszOczekujaceChallengeRekordy(oczekujace);[cite: 1]
                await zapiszChallengeRekordy(rekordy);[cite: 1]
            } else {[cite: 1]
                let oczekujace = [...pobierzOczekujaceRekordy()];[cite: 1]
                let rekordy = [...pobierzRekordy()];[cite: 1]
                let zatwierdzonyRec = oczekujace.splice(index, 1)[0];[cite: 1]
                rekordy.push(zatwierdzonyRec);[cite: 1]
                await zapiszOczekujaceRekordy(oczekujace);[cite: 1]
                await zapiszRekordy(rekordy);[cite: 1]
            }[cite: 1]
            zmienStrone('panel_moda');[cite: 1]
        }[cite: 1]

        async function odrzucRekord(index, kategoria = 'level') {[cite: 1]
            if (kategoria === 'challenge') {[cite: 1]
                let oczekujace = [...pobierzOczekujaceChallengeRekordy()];[cite: 1]
                oczekujace.splice(index, 1);[cite: 1]
                await zapiszOczekujaceChallengeRekordy(oczekujace);[cite: 1]
            } else {[cite: 1]
                let oczekujace = [...pobierzOczekujaceRekordy()];[cite: 1]
                oczekujace.splice(index, 1);[cite: 1]
                await zapiszOczekujaceRekordy(oczekujace);[cite: 1]
            }[cite: 1]
            zmienStrone('panel_moda');[cite: 1]
        }[cite: 1]

        async function usunZatwierdzonyPoziom(index, kategoria = 'level') {[cite: 1]
            if (confirm("Usunąć ten poziom?")) {[cite: 1]
                if (kategoria === 'challenge') {[cite: 1]
                    let lista = [...pobierzChallenge()];[cite: 1]
                    let usunietyLvl = lista.splice(index, 1)[0];[cite: 1]
                    await zapiszChallenge(lista);[cite: 1]
                    if (usunietyLvl) {[cite: 1]
                        let rekordy = pobierzChallengeRekordy();[cite: 1]
                        await zapiszChallengeRekordy(rekordy.filter(r => r.poziom.toLowerCase() !== usunietyLvl.nazwa.toLowerCase()));[cite: 1]
                    }[cite: 1]
                } else {[cite: 1]
                    let lista = [...pobierzPoziomy()];[cite: 1]
                    let usunietyLvl = lista.splice(index, 1)[0];[cite: 1]
                    await zapiszPoziomy(lista);[cite: 1]
                    if (usunietyLvl) {[cite: 1]
                        let rekordy = pobierzRekordy();[cite: 1]
                        await zapiszRekordy(rekordy.filter(r => r.poziom.toLowerCase() !== usunietyLvl.nazwa.toLowerCase()));[cite: 1]
                    }[cite: 1]
                }[cite: 1]
                zmienStrone('panel_moda');[cite: 1]
            }[cite: 1]
        }[cite: 1]

        async function usunZatwierdzonyPoziomLista(index, kategoria = 'level') {[cite: 1]
            if (confirm("Usunąć ten poziom?")) {[cite: 1]
                if (kategoria === 'challenge') {[cite: 1]
                    let lista = [...pobierzChallenge()];[cite: 1]
                    let usunietyLvl = lista.splice(index, 1)[0];[cite: 1]
                    await zapiszChallenge(lista);[cite: 1]
                    if (usunietyLvl) {[cite: 1]
                        let rekordy = pobierzChallengeRekordy();[cite: 1]
                        await zapiszChallengeRekordy(rekordy.filter(r => r.poziom.toLowerCase() !== usunietyLvl.nazwa.toLowerCase()));[cite: 1]
                    }[cite: 1]
                } else {[cite: 1]
                    let lista = [...pobierzPoziomy()];[cite: 1]
                    let usunietyLvl = lista.splice(index, 1)[0];[cite: 1]
                    await zapiszPoziomy(lista);[cite: 1]
                    if (usunietyLvl) {[cite: 1]
                        let rekordy = pobierzRekordy();[cite: 1]
                        await zapiszRekordy(rekordy.filter(r => r.poziom.toLowerCase() !== usunietyLvl.nazwa.toLowerCase()));[cite: 1]
                    }[cite: 1]
                }[cite: 1]
                zmienStrone('lista', kategoria);[cite: 1]
            }[cite: 1]
        }[cite: 1]

        async function usunZatwierdzonyRekord(index, kategoria = 'level') {[cite: 1]
            if (confirm("Usunąć ten rekord?")) {[cite: 1]
                if (kategoria === 'challenge') {[cite: 1]
                    let rekordy = [...pobierzChallengeRekordy()];[cite: 1]
                    rekordy.splice(index, 1);[cite: 1]
                    await zapiszChallengeRekordy(rekordy);[cite: 1]
                } else {[cite: 1]
                    let rekordy = [...pobierzRekordy()];[cite: 1]
                    rekordy.splice(index, 1);[cite: 1]
                    await zapiszRekordy(rekordy);[cite: 1]
                }[cite: 1]
                zmienStrone('panel_moda');[cite: 1]
            }[cite: 1]
        }[cite: 1]

        async function init() {[cite: 1]
            await fetchDataFromCloud();[cite: 1]
            odswiezMenu();[cite: 1]
            zmienStrone('home');[cite: 1]
        }[cite: 1]

        init();[cite: 1]
    </script>
</body>
</html>